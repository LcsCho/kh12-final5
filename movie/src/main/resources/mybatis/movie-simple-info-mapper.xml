<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="movieSimpleInfo">

	<!-- 조회 -->
	<select id="findAll" resultType="MovieSimpleInfoDto">
		select * from movie_simple_info where movie_no = #{movieNo}
	</select>

</mapper>
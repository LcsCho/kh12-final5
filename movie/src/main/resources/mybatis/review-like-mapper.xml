<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="reviewLike">

	<!-- 등록 -->
	<insert id="save">
		insert into review_like values (#{reviewNo}, #{member_nickname})
	</insert>
	
	<!-- 삭제 -->
	<delete id="edit">
		delete review_like 
			where review_no = #{reviewNo} 
				and meber_nickname = #{memberNickname} 
	</delete>
	
</mapper>
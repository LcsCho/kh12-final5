<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="review">
	
	<select id="sequence" resultType="int">
		select review_seq.nextval from dual
	</select>
	
	<!-- 등록 -->
	<insert id="save">
		insert into review (
			review_no, member_id, member_nickname, 
			movie_no, review_content
		) 
		values (
			#{reviewNo}, #{memberId}, #{memberNickname}, 
			#{movieNo}, #{reviewContent}
		)
	</insert>
	
	<!-- 수정 -->
	<update id="edit">
		update review 
		<set>
			<if test="reviewDto.reviewContent !=null"> review_content = #{reviewContent},</if>
		</set> 
		where review_no = #{reviewNo}
	</update>
	
	<!-- 삭제 -->
	<delete id="delete">
		delete review where review_no = #{reviewNo}
	</delete>
	
	<!-- 영화 번호로 리스트 조회 -->
	<select id="findAllByMovieNo" resultType="reviewDto">
		select * from (
			select rownum rn, TMP.* from(
				select * from review where movie_no = #{movieNo} order by review_like_count desc
			)TMP
		) where rn between 1 and 10
	</select>
	
</mapper>
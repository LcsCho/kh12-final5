<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="reviewList">
	
	<!-- 조회(최신순) -->
	<select id="findByDateDesc" resultType="ReviewListVO">
		select * from review_list order by review_date desc
	</select>
	
	<!-- 조회(오래된순) -->
	<select id="findByDateAsc" resultType="ReviewListVO">
		select * from review_list order by review_date asc
	</select>
	
	<!-- 조회(좋아요순) -->
	<select id="findByLikeDesc" resultType="ReviewListVO">
		select * from review_list order by review_like_count desc
	</select>
	
	<!-- 조회(평점높은순) -->
	<select id="findByRatingDesc" resultType="ReviewListVO">
		select * from review_list order by rating_score desc
	</select>
	
	<!-- 조회(평점낮은순) -->
	<select id="findByRatingAsc" resultType="ReviewListVO">
		select * from review_list order by rating_score asc
	</select>
	
</mapper>